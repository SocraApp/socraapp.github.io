let Point;try{({Point}=await import('https://cdn.jsdelivr.net/npm/@canvas-ui/core@2.0.0/+esm'))}catch{Point=class{constructor(x=0,y=0){this.x=x;this.y=y}}}const canvas=document.querySelector('#lost-canvas'),ctx=canvas.getContext('2d'),pointer=new Point(innerWidth/2,innerHeight/2);let w,h,dpr,phase=0,pulse=0;function resize(){dpr=Math.min(devicePixelRatio,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}function draw(){requestAnimationFrame(draw);phase+=.006;pulse*=.96;ctx.clearRect(0,0,w,h);for(let y=24;y<h;y+=34){ctx.beginPath();for(let x=0;x<w;x+=8){const dist=Math.hypot(x-pointer.x,y-pointer.y),wave=Math.sin(x*.018+phase*4+y*.006)*4+Math.sin(dist*.045-phase*8)*Math.max(0,38-dist*.06)+pulse*Math.sin(dist*.08)*22;const py=y+wave;if(!x)ctx.moveTo(x,py);else ctx.lineTo(x,py)}ctx.strokeStyle='rgba(240,238,231,.13)';ctx.stroke()}ctx.beginPath();ctx.arc(pointer.x,pointer.y,40+pulse*3,0,Math.PI*2);ctx.strokeStyle='rgba(199,255,50,.5)';ctx.stroke()}addEventListener('resize',resize);addEventListener('pointermove',e=>{pointer.x=e.clientX;pointer.y=e.clientY;document.querySelector('#lost-x').textContent=String(Math.round(e.clientX)).padStart(3,'0');document.querySelector('#lost-y').textContent=String(Math.round(e.clientY)).padStart(3,'0')});addEventListener('click',()=>pulse=1);resize();draw();if(window.gsap){gsap.from('header',{y:-90,duration:.8,ease:'power3.out'});gsap.from('.lost-kicker,h1 .lost-copy',{y:50,opacity:0,stagger:.12,duration:1,ease:'power3.out'});gsap.to('.lost-index button',{rotate:360,duration:24,repeat:-1,ease:'none'});gsap.to('.lost-index',{scale:1.04,duration:3,repeat:-1,yoyo:true,ease:'sine.inOut'})}
